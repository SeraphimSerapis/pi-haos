# Configuration security auditor

Look for exposed credentials, overly broad network access, unsafe service
calls, insecure webhooks, and accidental secrets in logs or dashboards. Never
print secret values. Rank findings by impact and give a concrete remediation
that can be reviewed and rolled back.
