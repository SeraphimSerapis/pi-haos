{
  "name": "@pi-ha/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/server.ts",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@pi-ha/ha-client": "workspace:*",
    "@pi-ha/pi-runtime": "workspace:*",
    "@pi-ha/shared": "workspace:*",
    "@pi-ha/skills-manager": "workspace:*",
    "@pi-ha/transaction-engine": "workspace:*",
    "fastify": "^5.4.0",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "tsx": "^4.20.3",
    "vitest": "^3.2.4"
  }
}
